// NPM es Gestor de paquetes por defecto de node js 
//$ npm install -g npm --> instalar de forma global 

// lodash es para manejos y utilitaroos de cadenasw de texto y numeros 
// $ npm install lodash --> con esto se instala lodash y se crea un archivo package.json
// y node_modules

// para no estar instalando en cada monmentpo el npm se hace 
// $ npm init esto nos ayuda para indicar los valores por defecto del aplicatico 
// al dejarlo por defcto se creo un archivo package.json

// para ionstalar lodash 
// $ npm i lodash --save indicamos que el modulo se instale en la aplicacion y a su vex en el archivo 
// package.json se actualice 

// al hacer esto nos ayuda porque le da gran portabilidad  alos proytectos 
// ya que la carpeta que se crea de node_modules lo podemos eliminar y dejar solo el .json 
// al subirlo aun repositorio solo es que cuandop lo descarguemos le hagamos 
// $ npm install y ahi nbuevamente se crea esa carpeta con el lodash y asi se pueda mover de un lugar a otro esto se logra con el --save 

// para desistalar en con 
//$ npm uninstall lodash --save y asi se elimina en la seccion dependecis